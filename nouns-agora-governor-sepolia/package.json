{
  "name": "nouns-agora-governor-sepolia",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ nouns-agora-governor-sepolia",
    "create-local": "graph create --node http://localhost:8020/ nouns-agora-governor-sepolia",
    "remove-local": "graph remove --node http://localhost:8020/ nouns-agora-governor-sepolia",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nouns-agora-governor-sepolia",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.60.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
